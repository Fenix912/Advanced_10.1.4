server.port: 4444

spring:
  cloud:
    stream:
      kafka:
        binder:
          auto-add-partitions: true

      bindings:
        checkBookEventProcessor-in-0:
          destination: store-book